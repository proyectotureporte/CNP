import InterAppScope from '@/components/layout/InterAppScope';

export default function PeritoLayout({ children }: { children: React.ReactNode }) {
  return <InterAppScope>{children}</InterAppScope>;
}
